const PI = 3.14;

let radius = 3;

area = radius * radius * PI;

radius = 20;

area = radius * radius * PI;

// display to terminal
// console.log(area);


const fav_foods = ["ice cream", "orange", "chocolate", "rice"];

//drops the last item
fav_foods.pop();

// drops the first item
fav_foods.shift(0);


// display to terminal
console.log(fav_foods)